name: review dog
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  some:
    runs-on: ubuntu-latest

    steps:
    - uses: reviewdog/action-setup@v1
      with:
        reviewdog_version: nightly
    - run: reviewdog -version
