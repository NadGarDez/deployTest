name: test-prettier-eslint
on: [push]

jobs:
  run-prettier-and-eslint:
    runs-on: ubuntu-latest
    steps:
      - name: start prettier and eslint
      - uses: actions/checkout@v2
        run: |
          npm install
          npm run prettier
          npm run eslint
