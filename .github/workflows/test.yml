name: test-prettier-eslint
on: [push]

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

        name : import proyect
      - run : "echo hola"
      - run : "node -v"
 
